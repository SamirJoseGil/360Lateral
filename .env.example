# ==============================================
# 🔧 CONFIGURACIÓN DE ENTORNO - LATERAL 360°
# ==============================================

# Aplicación
APP_NAME=Lateral 360°
DEBUG=1

# Base de datos PostgreSQL
DB_NAME=lateral360
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Django
SECRET_KEY=dev-secret-key-change-in-production
DJANGO_SETTINGS_MODULE=config.settings.development
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Puertos
BACKEND_PORT=8000
FRONTEND_PORT=3000

# Dominios (para producción)
BACKEND_DOMAIN=
FRONTEND_DOMAIN=

# Email (opcional)
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_USER=
EMAIL_PASSWORD=
EMAIL_USE_TLS=True

# ==============================================
# 🔐 EJEMPLO DE VARIABLES DE ENTORNO
# ==============================================
# Copia este archivo a .env y ajusta los valores

# Django Settings
DJANGO_ENV=development
DEBUG=True
SECRET_KEY=your-secret-key-here

# Database
DB_NAME=lateral360
DB_USER=postgres
DB_PASSWORD=your-password-here
DB_HOST=db
DB_PORT=5432

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# API URLs
VITE_API_URL=http://backend:8000/api
VITE_API_URL_EXTERNAL=http://localhost:8000/api
VITE_BACKEND_HOST=backend
VITE_BACKEND_PORT=8000

# Security
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,backend
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://frontend:3000
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://frontend:3000

# ==============================================
# 🔐 VARIABLES DE ENTORNO - LATERAL 360°
# ==============================================

# Environment
DJANGO_ENV=development
DEBUG=True

# Database
DB_NAME=lateral360_db
DB_USER=lateral360_user
DB_PASSWORD=lateral360_password
DB_HOST=db
DB_PORT=5432

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# Django
DJANGO_SECRET_KEY=your-secret-key-here-change-in-production
DJANGO_SETTINGS_MODULE=config.settings
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,backend,0.0.0.0

# Superuser
DJANGO_SUPERUSER_EMAIL=admin@lateral360.com
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_PASSWORD=admin123

# Optional: Create additional users
CREATE_ADDITIONAL_USERS=false

# Frontend
API_URL=http://localhost:8000
BACKEND_URL=http://backend:8000

# CORS (optional - override defaults)
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
# CORS_ALLOW_ALL=False

# JWT (optional - override defaults)
# JWT_ACCESS_MINUTES=60
# JWT_REFRESH_DAYS=7

# API Pagination
# API_PAGE_SIZE=20

# Session
# SESSION_AGE_SECONDS=3600

# File Upload
# MAX_UPLOAD_SIZE=10485760

# Email (for production)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password

# Admin
ADMIN_ENABLED=True
