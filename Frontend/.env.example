# =============================================================================
# 360Lateral Frontend - Variables de Entorno
# =============================================================================

# Node Environment
NODE_ENV=development

# API Configuration - Internal (Docker)
# URL usada por el servidor SSR para comunicarse con el backend
VITE_API_URL=http://localhost:8000

# API Configuration - External (Browser)
# URL usada por el navegador del cliente
VITE_API_URL_EXTERNAL=http://localhost:8000

# Backend Configuration (Legacy - mantenido por compatibilidad)
VITE_BACKEND_HOST=localhost
VITE_BACKEND_PORT=8000

# Session Configuration
SESSION_SECRET=your-secret-key-here-change-in-production

# =============================================================================
# Notas de Configuración
# =============================================================================
# 
# 1. VITE_API_URL: URL interna de Docker, usada por SSR
#    - Desarrollo: http://backend:8000
#    - Producción: http://backend-service:8000
#
# 2. VITE_API_URL_EXTERNAL: URL pública, usada por el navegador
#    - Desarrollo: http://localhost:8000
#    - Producción: https://api.tu-dominio.com
#
# 3. SESSION_SECRET: Clave secreta para firmar cookies
#    - Generar con: openssl rand -base64 32
#    - DEBE ser diferente en cada entorno
#    - NUNCA commitear la clave de producción
